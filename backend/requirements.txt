fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
# zoneinfo has no bundled tz database — it reads the OS's /usr/share/zoneinfo,
# which slim container images don't ship. This package supplies the IANA
# database directly so timezone lookups work the same in every environment.
tzdata==2024.2
# Sync driver, used only by Alembic. Migrations run as the database owner while
# the app runs as a separate non-owner role, so the two drivers are separate.
psycopg[binary]==3.2.3
alembic==1.14.0
pgvector==0.3.6

argon2-cffi==23.1.0
pyjwt==2.10.1
cryptography==44.0.0
pyotp==2.9.0
qrcode[pil]==8.0

httpx==0.28.1
python-multipart==0.0.20
jinja2==3.1.5
# Optional: only imported when REDIS_URL is set.
redis==5.2.1

xlsxwriter==3.2.0
# ReportLab rather than WeasyPrint: WeasyPrint needs the GTK/Pango/Cairo native
# stack, which is a genuine install burden on Windows and a recurring support
# cost for a self-hosted product. ReportLab is pure Python wheels everywhere.
reportlab==4.2.5
matplotlib==3.10.0

# Predictive analytics (Module H.2). Deliberately scikit-learn rather than an
# LLM: win probability and trend forecasting are regression problems over
# structured history, and a language model asked to produce a percentage will
# produce a confident one whether or not the data supports it.
scikit-learn==1.6.0

structlog==24.4.0
